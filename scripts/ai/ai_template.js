{
    init: function (ccblock) {

        this.state = {};

        // component = this.get(tag)
        // this.each(function(component){ ... });

        // this.d1 = this.distance_sensor({
        //    refObject: ccblock,
        //    refOffset: null,       // (0,0)
        //    distance:  [1.0, 5.0],
        //    angle:     [-Math.PI/8, Math.PI/8], // null for full range
        //    friendly:  true,
        //    enemy:     true,
        //    neutral:   false
        // });

    },

    update: function (dt, keyDown) {

        // if (this.d1.found &&
        //         this.d1.first.enemy &&
        //         this.d1.first.obj.type !== 'spikes') {
        //     this.log('Charging');
        //     keyDown[BSWG.KEY.UP] = true;
        //     this.hold(3.0);
        //     this.pause(1.0);
        // }

    }
}